# 项目结构与新手入门手册

> 项目名称：yu-ai-agent（AI 恋爱顾问）
> 技术栈：Spring Boot 3.5.14 + Spring AI + 阿里云百炼 DashScope
> 模型：deepseek-v4-flash

---

## 一、项目目录结构

```
yu-ai-agent/
├── pom.xml                                    # Maven 依赖管理（核心依赖定义）
│
├── src/main/java/com/tripo/yuaiagent/
│   ├── YuAiAgentApplication.java              # Spring Boot 入口，启动类
│   │
│   ├── app/
│   │   └── LoveApp.java                       # ★ 核心业务类：聊天、报告、RAG、推荐
│   │
│   ├── advisor/
│   │   ├── MyLoggerAdvisor.java               # 自定义日志拦截器（打印请求/响应日志）
│   │   └── ReReadingAdvisor.java              # Re2（重复阅读）优化提示词
│   │
│   ├── rag/
│   │   ├── LoveAppDocumentLoader.java         # 加载 document/*.md 文件到系统
│   │   ├── LoveAppVectorStoreConfig.java      # ★ 本地向量数据库配置（SimpleVectorStore）
│   │   └── LoveAppRagCloudAdvisorConfig.java  # ★ 云知识库配置（阿里云 DashScope）
│   │
│   ├── ChatMemory/
│   │   └── FileBasedChatMemory.java           # 基于文件的聊天记录持久化（存磁盘.kryo文件）
│   │
│   ├── controller/
│   │   └── HealthyController.java             # 健康检查接口（GET /api/healthy）
│   │
│   └── demo/invoke/
│       ├── SdkAiInvoke.java                   # 直接用 DashScope SDK 调 AI（无框架）
│       ├── HttpAiInvoke.java                  # 用 HTTP + Hutool 调 AI（无框架）
│       ├── SpringAiInvoke.java                # 用 Spring AI 初步调 AI（命令行）
│       └── TestApiKey.java                    # API Key 测试文件
│
├── src/main/resources/
│   ├── application.yml                        # 主配置（端口8123、Swagger、日志）
│   ├── application-local.yml                  # 本地配置（API Key、模型名）
│   └── document/
│       ├── 恋爱常见问题和回答 - 恋爱篇.md
│       ├── 恋爱常见问题和回答 - 单身篇.md
│       ├── 恋爱常见问题和回答 - 已婚篇.md
│       └── 恋爱对象库.md                      # ★ 你新增的恋爱对象推荐数据
│
└── src/test/java/com/tripo/yuaiagent/
    ├── YuAiAgentApplicationTests.java         # 启动测试
    ├── app/LoveAppTest.java                   # ★ 功能集成测试（聊天/RAG/推荐）
    └── rag/LoveAppDocumentLoaderTest.java     # 文档加载测试
```

---

## 二、从零搭建这个项目的步骤

### 第 1 步：创建 Spring Boot 项目

用 Spring Initializr 或 IDEA 创建 Spring Boot 3.5.14 项目，Java 21。

### 第 2 步：加入 Spring AI + 阿里云 DashScope 依赖（pom.xml）

核心依赖就两个：

```xml
<!-- DashScope 官方 Starter，一行引入整个 Spring AI + 阿里云 -->
<dependency>
    <groupId>com.alibaba.cloud.ai</groupId>
    <artifactId>spring-ai-alibaba-starter-dashscope</artifactId>
    <version>1.1.2.1</version>
</dependency>
```

这玩意儿一加，你就能用 `ChatModel`、`ChatClient`、`EmbeddingModel`、`VectorStore` 等全套 Spring AI 能力，底层的 API 调用、鉴权、流式处理全被框架包了。

还加了几个辅助库：

| 依赖 | 干什么的 |
|------|----------|
| `lombok` | 少写 `getter/setter/log` 样板代码 |
| `hutool-all` | HTTP 请求、JSON 处理等工具集 |
| `knife4j` | Swagger UI 接口文档页面 |
| `kryo` | Java 对象序列化（存聊天记录到文件） |
| `jsonschema-generator` | 让 AI 输出结构化 JSON |
| `spring-ai-markdown-document-reader` | 读取 Markdown 文件转成 Document 对象 |
| `spring-ai-advisors-vector-store` | RAG 用的 QuestionAnswerAdvisor |

### 第 3 步：配 API Key（application-local.yml）

```yaml
spring:
  ai:
    dashscope:
      api-key: sk-xxx  # 你的阿里云百炼 API Key
      chat:
        options:
          model: deepseek-v4-flash
```

**原理**：Spring AI 的自动配置（AutoConfiguration）扫描到这个配置后，会自动创建 `ChatModel`、`EmbeddingModel` 等 Bean，你不用自己 new。

### 第 4 步：先裸调模型验证连通性（demo 包）

这是四个逐渐进阶的示例，建议按顺序看：

1. **`HttpAiInvoke.java`** — 最底层，直接 HTTP POST 调阿里云接口，没有任何框架
2. **`SdkAiInvoke.java`** — 用阿里云官方 SDK 调，封装了 HTTP 细节
3. **`SpringAiInvoke.java`** — 用 Spring AI 的 `ChatModel.call()`，一行代码调模型
4. **`LoveApp.java`** — 真正的业务代码

### 第 5 步：核心业务 — LoveApp.java

这是大头。它用 `ChatClient`（Spring AI 的高级 API）来和大模型对话。

```java
// ChatClient 是线程安全的，构建一次，到处重用
/*
chatClient = ChatClient.builder(dashscopeChatModel)
    .defaultSystem(SYSTEM_PROMPT)           // 所有对话都带的系统提示词
    .defaultAdvisors(myLoggerAdvisor, ...)  // 全局拦截器
    .build();*/

```

目前实现了 4 个功能：

| 方法 | 功能 | 技术点 |
|------|------|--------|
| `doChat()` | 普通聊天带记忆 | ChatMemory + MessageChatMemoryAdvisor |
| `doChatWithReport()` | 生成结构化恋爱报告 | `.entity()` 自动解析 JSON → Java 对象 |
| `doChatWithRag()` | 基于云知识库问答 | RetrievalAugmentationAdvisor |
| `recommendPartner()` | ★ 基于本地向量库推荐对象 | QuestionAnswerAdvisor + SimpleVectorStore |

### 第 6 步：RAG 知识库（rag 包）

两套 RAG 方案并存：

| | 本地方案 | 云方案 |
|---|---|---|
| **配置类** | `LoveAppVectorStoreConfig` | `LoveAppRagCloudAdvisorConfig` |
| **存储** | `SimpleVectorStore`（内存） | 阿里云百炼知识库 |
| **数据来源** | `document/*.md` 本地文件 | 上传到阿里云控制台 |
| **检索方式** | `QuestionAnswerAdvisor` | `DashScopeDocumentRetriever` |
| **谁在用** | `recommendPartner()` | `doChatWithRag()` |

### 第 7 步：测试运行

`LoveAppTest.java` 里每个方法对应一个功能测试，用 `@SpringBootTest` 启动完整 Spring 上下文来跑。

---

## 三、一条请求的完整流程（以 recommendPartner 为例）

```
你（用户）说："推荐一个25岁的程序员"
         │
         ▼
  LoveApp.recommendPartner("推荐一个25岁的程序员")
         │
         │  ChatClient.prompt().user(...).advisors(...).call()
         │
         ├── 第 1 站：MyLoggerAdvisor（记录请求日志）
         │
         ├── 第 2 站：QuestionAnswerAdvisor
         │     │
         │     ├── 去 SimpleVectorStore 做相似度搜索
         │     │      ("25岁" + "程序员" 的语义向量 ≈ 张三的数据)
         │     │
         │     └── 把检索到的张三资料拼到 prompt 里
         │
         ├── 第 3 站：ChatModel（DashScope API）
         │     │
         │     └── DeepSeek 模型看到：
         │          System: "你是一位专业恋爱情感顾问..."
         │          User: "推荐一个25岁的程序员"
         │          Context: "张三，28岁，天蝎座，程序员..."
         │
         ├── 第 4 站：MyLoggerAdvisor（记录响应日志）
         │
         └── 返回结果："我推荐张三给你，28岁程序员..."
```

### 关键概念白话解释

| 概念 | 一句话解释 |
|------|------------|
| **ChatModel** | 大模型的 Java 接口。相当于你给模型打电话的听筒 |
| **ChatClient** | 比 ChatModel 更好用的高级 API，支持链式调用 `.prompt().user().call()` |
| **Advisor** | 拦截器/过滤器。在发请求给模型前后插入逻辑（加记忆、加知识库、打日志） |
| **VectorStore** | 向量数据库。存的是"语义向量"而不是原始文本 |
| **Embedding** | 把文字转成一串数字（向量）。"程序员"和"写代码"的向量距离很近 |
| **RAG** | 检索增强生成。先查知识库，再把查到的内容塞给模型，让它基于资料回答 |
| **QuestionAnswerAdvisor** | 一个现成的 Advisor，自动做"查向量库→拼到 prompt→返回答案" |
| **SimpleVectorStore** | 最简单的向量库，数据放内存，重启就没了 |

---

## 四、快速扩展指南（再添新功能怎么搞）

### 想再加一个新的 RAG 知识库（比如"星座配对库"）

**只需要 2 步：**

1. 在 `document/` 下新建 `星座配对库.md`
2. 重启项目 —— `LoveAppDocumentLoader` 自动加载所有 `*.md`

`LoveAppVectorStoreConfig` 已经在启动时把全部文档塞进 `SimpleVectorStore` 了。

### 想把这两个知识库分开（FAQ 归 FAQ，对象库归对象库）

新建一个独立的 VectorStore Bean：

```java
@Bean
VectorStore partnerVectorStore(EmbeddingModel embeddingModel,
                               LoveAppDocumentLoader loader) {
    // 只加载恋爱对象库.md
    List<Document> docs = loader.loadSingleFile("恋爱对象库.md");
    SimpleVectorStore store = SimpleVectorStore.builder(embeddingModel).build();
    store.add(docs);
    return store;
}
```

然后在 `recommendPartner()` 里用 `partnerVectorStore` 而不是 `loveAppVectorStore`。

### 想加一个新的 AI 功能

模式固定：

```java
/*
public String 你的新功能(String message) {
    return chatClient
            .prompt()
            .system("你的专属提示词")  // 可选，覆盖默认系统提示词
            .user(message)
            .advisors(...)             // 可选，加 RAG/记忆等
            .call()
            .content();
}*/

```

---

## 五、你现在处于什么阶段

```
裸调 API（HttpAiInvoke）     →  了解最底层原理
     │
     ▼
用 SDK 调（SdkAiInvoke）    →  了解 SDK 封装
     │
     ▼
用 Spring AI 调（SpringAiInvoke） →  了解框架用法
     │
     ▼
★ 你现在在这里：LoveApp.java     →  用 ChatClient 编排业务流程
     │
     ▼
下一步：把功能暴露成 REST 接口  →  用 @RestController 包装 LoveApp
     │
     ▼
再下一步：整合前端页面          →  用 Thymeleaf / Vue 写聊天界面
```

你现在已经站在第四层了，上面几层是 demo 示例帮你理解。核心要记住的就是 **ChatClient 是大脑，Advisor 是左右手**，搞定这俩就搞定了这个项目。